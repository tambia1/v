import styled from "styled-components";

export const Transition = styled.div`
	position: relative;
	width: 100%;
	height: 100%;
`;

export const Child = styled.div`
	position: absolute;
	inset: 0;
`;
