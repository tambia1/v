import styled from "styled-components";

export const Button = styled.div`
	width: 4rem;
	height: 4rem;
	cursor: pointer;
	display: flex;
	flex-shrink: 0;
	align-items: center;
	justify-content: center;
`;
